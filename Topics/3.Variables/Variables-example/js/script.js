/**
 * Variable learning
 * Junming He
 * 
 */

"use strict";

/**
 * Canvas * 3
*/
function setup() {
    createCanvas(640, 640);
}


/**
 * OOPS I DIDN'T DESCRIBE WHAT MY DRAW DOES!
*/
function draw() {
    background(0);

    //draw something
    push();
    fill(255, 255, 0);
    noStroke();
    ellipse(width / 2, height / 2, 100, 100);
    pop();
}